const chai = require('chai')
chai.should()


describe('name or description of test', () => {
  // beforeEach ( () =>{})
  // afterEach ( () =>{})
  it('${true should be true}', () => {
    true.should.be.true
  })
})